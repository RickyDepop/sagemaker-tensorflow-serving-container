diff --git a/docker/1.15/Dockerfile.cpu b/docker/1.15/Dockerfile.cpu
index 0b304a7..eeeaaf1 100644
--- a/docker/1.15/Dockerfile.cpu
+++ b/docker/1.15/Dockerfile.cpu
@@ -1,3 +1,8 @@
+################################################################################################################################################
+# provide support for ScaNN Tensoflor operator
+ARG TF_SERVING_BUILD_IMAGE=google/tf-serving-scann:2.6.0-devel
+FROM ${TF_SERVING_BUILD_IMAGE} as build_image
+################################################################################################################################################
 FROM ubuntu:18.04
 
 LABEL maintainer="Amazon AI"
@@ -11,7 +16,9 @@ ARG PYTHON=python3
 ARG PIP=pip3
 ARG TFS_SHORT_VERSION=1.15.2
 ARG TF_S3_URL=https://tensorflow-aws.s3-us-west-2.amazonaws.com
-ARG TF_MODEL_SERVER_SOURCE=${TF_S3_URL}/${TFS_SHORT_VERSION}/Serving/CPU-WITH-MKL/tensorflow_model_server
+################################################################################################################################################
+# ARG TF_MODEL_SERVER_SOURCE=${TF_S3_URL}/${TFS_SHORT_VERSION}/Serving/CPU-WITH-MKL/tensorflow_model_server
+################################################################################################################################################
 
 # See http://bugs.python.org/issue19846
 ENV LANG=C.UTF-8
@@ -79,8 +86,20 @@ RUN ln -s $(which ${PYTHON}) /usr/local/bin/python \
 RUN curl ${TF_S3_URL}/MKL-Libraries/libiomp5.so -o /usr/local/lib/libiomp5.so \
  && curl ${TF_S3_URL}/MKL-Libraries/libmklml_intel.so -o /usr/local/lib/libmklml_intel.so
 
-RUN curl ${TF_MODEL_SERVER_SOURCE} -o /usr/bin/tensorflow_model_server \
- && chmod 555 /usr/bin/tensorflow_model_server
+################################################################################################################################################
+# RUN curl ${TF_MODEL_SERVER_SOURCE} -o /usr/bin/tensorflow_model_server \
+#  && chmod 555 /usr/bin/tensorflow_model_server
+################################################################################################################################################
+RUN apt-get update && apt-get install -y --no-install-recommends \
+    software-properties-common && add-apt-repository -y ppa:ubuntu-toolchain-r/test && \
+    apt-get update && apt-get install -y g++-9 && \
+    apt-get clean && \
+    rm -rf /var/lib/apt/lists/*
+
+# Install TF Serving pkg
+COPY --from=build_image /usr/local/bin/tensorflow_model_server /usr/bin/tensorflow_model_server
+RUN chmod 555 /usr/bin/tensorflow_model_server
+################################################################################################################################################
 
 # Expose ports
 # gRPC and REST
